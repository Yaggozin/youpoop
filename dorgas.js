// Ação a ser feitafunction dorgasMode() {    document.body.style.filter = "invert(1) hue-rotate(180deg)";    setTimeout(() => {        document.body.style.filter = "none";    }, 5000); // 0.5 segundo}// Sequência do Konami Code (setas e teclas A e B)const konamiCode = '38,38,40,40,37,39,37,39,66,65';let teclasPressionadas = [];document.addEventListener('keyup', (e) => {    teclasPressionadas.push(e.keyCode);    teclasPressionadas = teclasPressionadas.slice(-10); // Mantém apenas os últimos 10    if (teclasPressionadas.toString().includes(konamiCode)) {        dorgasMode(); // Executa a função do easter egg    }});